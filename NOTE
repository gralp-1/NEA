currently segfauting because the input data is all messed up, we need a new input image by now anyway
- fix file output
- select file format in the config window
- fully translate the config window
- formattable Translatef function
- fix converting FileFormat to a string for extension of output file
